#include <bits/stdc++.h> 
#include <algorithm>
void insertionSort(int n, vector<int> &arr){
    // Write your code here.
    for(int i=0;i<n-1;){
        if(arr[i]>arr[i+1]){
            swap(arr[i],arr[i+1]);
            i=0;
            continue;
        }i++;
    }
}
